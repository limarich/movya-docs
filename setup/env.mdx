---
title: "Configuração do .env"
description: "Variáveis de ambiente necessárias para o funcionamento do app"
---

## ⚙️ Configuração do Arquivo `.env`

Para o funcionamento correto do **Supere App**, é necessário criar um arquivo `.env` na raiz do projeto com a seguinte estrutura:

```env
EXPO_PUBLIC_MAPBOX_DOWNLOAD_TOKEN= informe_aqui_sua_chave_do_mapbox
```

---

## 🔑 Como obter a chave do Mapbox

- Acesse o site Mapbox.

- Crie ou faça login em sua conta.

- Localize o Access token na sua conta.

- Copie o token e cole no .env conforme o exemplo acima.

<Note> Certifique-se de **nunca** compartilhar seu `.env` publicamente ou versioná-lo no GitHub. </Note>